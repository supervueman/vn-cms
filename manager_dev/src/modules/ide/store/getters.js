const getters = {
  getLayout(state) {
    return state.layout;
  },
  getComponent(state) {
    return state.component;
  },
  getPage(state) {
    return state.page;
  },
};

export default getters;
