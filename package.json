{
  "name": "dashboard-admin-panel-blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "manager:dev:create:symlink": "cd manager_dev/public/static && ln -s ../../../files",
    "manager:dev:remove:symlink": "cd manager_dev/public/static && rm -r files",
    "manager:dev": "cd manager_dev && npm run serve",
    "manager:build": "cd manager_dev && npm run build",
    "manager:prod:create:symlink": "cd manager && mkdir static && cd static && ln -s ../../files",
    "manager:start:local": "serve -s manager",
    "client:dev:create:symlink": "cd client/static && ln -s ../../files",
    "client:dev:remove:symlink": "cd client/static && rm -r files",
    "client:dev": "cd client && npm run dev",
    "client:build": "cd client && npm run build",
    "client:start": "cd client && npm run start",
    "server:dev": "cd server && npm start",
    "start:dev": "npm run manager_dev:dev & npm run server:dev"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@gitlab.com/multicms/platform-cms.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://gitlab.com/multicms/platform-cms/issues"
  },
  "homepage": "https://gitlab.com/multicms/platform-cms#readme",
  "devDependencies": {},
  "dependencies": {
    "sass": "^1.22.12"
  }
}
