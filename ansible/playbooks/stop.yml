---
- hosts: all
  become: yes

  vars_files:
    - ../group_vars/global_vars.yml

  tasks:
    - name: Stop APP
      command: npm run app:stop:pm2 chdir={{homeDir}}/{{appDir}}
      ignore_errors: yes
